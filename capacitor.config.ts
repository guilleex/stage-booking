import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vdsystem.stagebooking',
  appName: 'stage-booking',
  webDir: 'dist'
};

export default config;
